version: 1
frontend:
  phases:
    preBuild:
      commands:
        - curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
        - apt-get install -y nodejs
        - npm install  # ✅ This ensures npm is used
    build:
      commands:
        - npm run build
